# Name,   Type, SubType, Offset,  Size,    Flags
#
# 8 MB layout — V1 and V2, whose U1 is a W25Q64 (see sdkconfig.defaults).
# V3 uses partitions_v3.csv instead, selected together with the 16 MB flash
# declaration by sdkconfig.defaults.v3; the two MUST move together, because
# gen_esp32part.py refuses a table that does not fit the declared flash size.
# That refusal is the good kind of failure and it is worth keeping: it is the
# only build-time check standing between a partition layout and a board that
# cannot hold it.
#
# spiffs ends exactly at 0x800000, the end of the part. This is the layout
# #835 item 1 grew to 0x9F0000 for the V3 board, where there is no external
# NAND and spiffs is the ONLY log store. V2 has the FORESEE F35SQB004G on
# SPI3 (board_v2.h, HAS_EXT_NAND = true) and logs there, so spiffs is a
# fallback and 1.94 MB is not the constraint it is on V3. V1 has neither NAND
# nor a bigger part; 1.94 MB is simply all there is.
nvs,      data, nvs,     0x9000,  0x5000,
otadata,  data, ota,     0xe000,  0x2000,
ota_0,    app,  ota_0,   0x10000, 0x300000,
ota_1,    app,  ota_1,   0x310000,0x300000,
spiffs,   data, spiffs,  0x610000,0x1F0000,
