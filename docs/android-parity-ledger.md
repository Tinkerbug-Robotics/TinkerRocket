# Android Parity Ledger

The running divergence record between the iOS and Android apps during (and
after) the port — android-port plan §2.3. Two kinds of entries:

- **Feature lag** — a capability one app has and the other doesn't yet.
- **Behavior divergence** — twin logic that intentionally or accidentally
  differs (a bugfix landed on one side, a policy change, a platform quirk).

Rules: any change under `TinkerRocketApp/TinkerRocketApp/Models/` or
`TinkerRocketAndroid/core/` that alters shared behavior must either touch the
twin in the same PR or add a dated entry here. Entries are removed when parity
is restored. "Parity at v1.0" is checkable ⇔ this file is honest.

## Feature lag (Android behind iOS — the port burn-down)

| Since | Area | Status |
|---|---|---|
| 2026-07-23 | `:core:protocol` COMPLETE — decoders, encoders, converter, telemetry, CSV pipeline all golden-pinned (bin→CSV byte-identical to iOS) | Phase 1 done |
| 2026-07-23 | Everything above the protocol layer: `:core:session` device/fleet logic, `:core:ble`, all UI | Phases 2–9 of docs/plans/624-android-port.md |
| 2026-08-16 | **Stand-back pyro test over LoRa** is iOS-only: on a base-station link, `sendPyroContTest`/`sendPyroFire` wrap in relay cmd 50 (inner 35/36 → new uplink handlers on OC + mini), gated on `pyroCommandPathReady` (focused rocket + relay ≤ 3 s fresh); continuity on the relay path reads the SH pyro scorecard (the LoRa downlink has no pyro_status byte); BS Settings gains a "Pyro Test (via LoRa)" section opening the slow-mo PyroTestView, which re-tests continuity post-fire for a stand-back fired/not-fired verdict. Android's `PyroTestControls` never ported the fire button at all (its own safety-sensitive pass, above) — port the LoRa path together with that pass, reusing the same uplink cmds and the SH-scorecard continuity read | iOS-first |
| 2026-08-17 | **Measured-continuity scorecard bits** (`SH_PYRO_MEAS_SHIFT` = {24,26,28,30}, previously reserved) are decoded on BOTH platforms — `TelemetryData.pyroMeasuredContinuity` exists in `:core:protocol` and in iOS — but only iOS renders them, in the pyro cards and the LoRa test. Bench-found: the pre-existing `SH_PYRO_SHIFT` bits are config-gated (`if (pyroChEnabled(i))`) while the direct-BLE `pyro_status` CONT bit is not, so a ground test on a channel not armed for flight read CONT over Bluetooth and "no reading" over LoRa. The gate could not simply be dropped — any `.bad` pyro is a hard "Do not fly" in `flightReadiness` on both platforms, so a bench-tested empty channel would ground the rocket. Hence a second, ungated field; the gated bits keep their go/no-go meaning untouched. Android parity is at the protocol layer only until its pyro UI pass | iOS-first (protocol: both) |

## Behavior divergences

| Since | Divergence | Rationale |
|---|---|---|
| 2026-07-23 | Disconnect suppression is **per-device** (`pendingUserDisconnects` set); iOS uses one global `userInitiatedDisconnect` flag that can suppress the WRONG device's reconnect in a multi-device fleet | The global flag is an iOS latent bug, not a behavior to preserve |
| 2026-07-23 | Download API returns typed `DownloadResult.Busy` for a second concurrent download; iOS silently overwrites the in-flight completion handler (leaking it) | Silent handler loss is a defect; byte/wire behavior unchanged |
| 2026-07-23 | Reconnect endgame is **re-scan → sighting-gated autoConnect**; iOS parks a CoreBluetooth `connect()` forever | Android `autoConnect=true` from a bare MAC can silently never fire (plan §3); the CB parked connect has no analog |
| 2026-07-27 | FreqScan's fixed-frequency **Apply** (transactional relay + verify + rollback) is iOS-only; Android shows a "use iOS" note | The fleet runs hopping mode (#150 default) where Apply is refused by design — the mask auto-applies. Port the transactional flow when a fixed-frequency need reappears |
| 2026-07-23 | Scan restart within 15 s is NOT truncated (epoch-guarded timeout); iOS's unguarded timer truncates a restarted scan | The iOS behavior is a timer-hygiene bug; a restarted scan deserves its full window |
| 2026-07-23 | KnownDeviceStore device list sorts plain case-insensitive; iOS uses `localizedCaseInsensitiveCompare` | Locale-dependent ordering is cosmetic; plain ordering is deterministic across devices |
| 2026-07-30 | Virtual Rocket is on BOTH platforms (Android `DemoMode.kt`, iOS `VirtualRocket.swift` — same script, frame for frame; one flight per SIM_START via the standard Simulation UI, SIM_STOP honored). iOS runs it in the Simulator (no CoreBluetooth). Divergence: the pre-staged downloadable sample flight is Android-only — iOS has no transport fake to serve file ops | Files-in-virtual: Android-only | Android's demo rides FakeFirmware (a full transport); iOS's rides a command tap + direct demux feed, which covers everything except the file pipeline. Follow-up noted: suppress the provisioning sheet for the virtual device (registry hygiene) |
| 2026-07-30 | In-app update checker (GitHub Releases → scanner banner → browser) is Android-only | Distribution differs by design (plan §1): iOS updates ride the App Store; Android sideloads from GitHub Releases, so the app itself must notice a new `android-v*` tag. Pure logic in `UpdateCheck.kt` (JVM-tested); fetch is throttled to one hit/day and silent on every failure — no signal at a launch site is normal |

## Feature lag (session layer, deferred to Phase 3)

| Since | Area | Status |
|---|---|---|
| ~~2026-07-23~~ | ~~RocketRoster merge + the 10 RocketRosterTests cases~~ | **PORTED 2026-07-27** — RocketRoster.kt + all 10 cases green; single-BS relay UI live (multi-device fleet chrome remains Phase 9) |

## UI parity (both directions — see docs/design-language.md)

| Since | Item | Direction | Status |
|---|---|---|---|
| 2026-08-12 | **78 hardcoded Material colors converted to design tokens** across 12 Android screens. Not a tidy-up: the literals were Material Design values (#43A047 Green 600, #B00020 error, #1E88E5 Blue 600…) that had never been converted, so Material greens rendered beside iOS-palette ones on the same card. 12 roles added (`accent`, the `statusOk/Marginal/Warn/Bad/Pending/Active` ladder, `pyroFired`, `deviceRocket`/`deviceBaseStation`, `launchSite`/`landingSite`), replacing what would have been nine per-screen triads for one meaning. **Twelve real divergences fixed**, each Android signalling a different meaning than iOS: the profile sync ladder was inverted (gray on awaiting-sync where iOS is orange — Android's own comment cites #375, the fix the color cancelled); the base-station badge was teal, which means Ground Test in this system, where iOS is orange; an infeasible Drift Cast read as merely degraded rather than red; the caution rung was Material Yellow where iOS is orange, at three sites; the mag-cal chip-limit warning sat on the explanatory paragraph while the number actually near the limit carried no color; and the verify gates rendered **green before any sample arrived** (`pass: Boolean` with `x == null || …`) — now tri-state. Deliberate keeps: the shared lit-green flag chip (#2E7D32, hardcoded identically on both platforms on purpose), the six-color chart series palette (categorical, not status), and the ten 3D scene literals (exact round-trips of iOS's SceneKit UIColors, none of which is a palette entry — tokenising them would *create* a divergence). Two solid fills became tinted (staleness banner, sync pill): white-on-solid was legible against Material 700/800 but drops to ~2.2:1 on the lighter palette  **Second population, same defect class:** 19 more Material hexes lived in MapLibre *string* form (`lineColor("#43A047")`), which the `Color(0x…)` audit did not match — MapScreen was briefly half-converted, its prediction badge on `statusOk` while its landing circle stayed Material green. Converted via the `@ColorInt` overload (`lineColor(tr.landingSite.toArgb())`). That pass found the **Drift Cast overlay scheme rotated on every element**: Android drew launch blue / landing RED / guidance purple with the two lines carrying each other's dash pattern, where iOS draws launch red / landing green / guidance blue, boost dashed blue, descent solid purple. A red landing marker is iOS's launch-pad colour — with two phones out at a launch that is a recovery hazard, the same one FlightTrajectoryScreen records fixing. Style installs are now keyed on the theme so overlays repaint on a light/dark flip; Drift Cast's effect had to be **split** rather than re-keyed, because MapLibre cannot unregister a click listener and a theme flip would have stacked one per flip. Kept literal: the white marker rings (chrome against imagery, which does not theme) and `#ededed` (iOS's placeholder substrate, `CachingTileOverlay.swift:27`) | iOS → Android | **code + build green; bench pending** |
| 2026-07-27 | Flight-event flag chips (LAUNCH/BURNOUT/APOGEE/LANDED/LOG illuminating) | Android → iOS | **done 2026-07-30** (`FlightEventFlagsView`, render-tested) |
| 2026-07-27 | Sensor-health dot row (per-subsystem green/amber/red/gray dots) | Android → iOS | **done 2026-07-30** (`HealthDotRow` replaces the labeled grid). The go/no-go banner completed the round trip the same day — Android now renders `flightReadiness` too (`ReadinessBanner`), so the health cards match in both directions |
| 2026-07-31 | **Dashboard missing-section fix** (user side-by-side review caught it): the reorder had matched order but not CONTENT — Android was missing the Signal bars card, the Battery table (charge/voltage/current), the whole IMU card, and the GPS row. All four ported; summary placement now follows iOS's per-link split (BS: after banners; direct: after IMU); pyro card now direct-links-only ("Pyro Channels" 2×2, iOS form); Start Logging orange. Remaining gaps: Ground Test screen (PyroTestView port — safety-sensitive, own pass), virtual-rocket script doesn't feed IMU triplets (demo polish). ~~per-channel "Disabled"/trigger text + Test Continuity, direct-link IMU nose annotation~~ **CLEARED 2026-08-09** — the "0xB1 config readback" label was a misnomer (0xB1 is the OC↔FC I2C `PID_CONFIG_MSG`, unrelated): the cmd-20 JSON readback (`config_pyro` / `imu_orient`) was already parsed and folded by `:core:session`; only dashboard wiring was missing. Landed as the iOS tile form (FIRED/CONT/NO CONT badge ladder, TESTING spinner during the 2.5 s cmd-35 window, trigger text via `UnitFormatter`), nose annotation with mode label + off-axis orange on both link types, High-G row hidden on BS links, and Imperial g-conversion for acceleration rows (two extra IMU-card parity misses caught in passing). Note: none of this renders in demo mode — the virtual device is a base station and the pyro card is direct-link-only | iOS → Android | verified against the user's iPhone screenshots via virtual rocket |
| 2026-07-31 | **Dashboard section order settled**: iOS layout is canon (user decision), Android reordered to it — flight summary right under power, Status before the flag chips, tools last, storage bar moved to the Files screen (its iOS home). The ONE deliberate exception: Android keeps the full dashboard with the power section near the top in the OFF state (iOS swaps to a battery+PowerOn-only view). Direct-link summary placement follows the BS-link position (iOS splits them; the flown-with BS layout wins for consistency) | iOS → Android (+doc) | design-language.md updated; iOS unchanged |
| 2026-07-30 | **Connected chrome + dashboard re-skin** (design pass, overnight batch): iOS toolbar arrangement replaces the text-tab strip (chevron + units menu leading; book/map/voice-speaker/gear trailing; chevron = back-to-dashboard from tabs, disconnect from dashboard — iOS root-chevron double duty); voice becomes the speaker icon with session-health tint (long-press test). Dashboard: iOS `RocketStateView` twin lands **with the #382 READY→PRELAUNCH display mapping Android never had**, plus the `FlightSummaryView` Current/Max table (altitude/speed/attitude) and iOS semantic colors on the Tools row. Held for morning review: dashboard section ORDER (iOS's own order differs from the design-language settled order Android follows), hop badges on the banner, Files/Settings depth | iOS → Android | verified on-device (virtual rocket, both halves screenshotted) |
| 2026-07-30 | **Display units + branding** (iOS #160 twin): global Metric/Imperial via the scanner's ruler menu (iOS toolbar mirror), persisted under the same key/values (`unitSystem`: metric/imperial), `UnitFormatter` ported format-string for format-string into `:core:session` (pinned by `UnitFormatterTest`), announcer speaks the setting, dashboard live values convert (alt cards, bearing distance/up-down). Tinkerbug logo tops the scanner right, same asset as iOS. Chart axes + planning tools stay SI until their design-pass turn | iOS → Android | verified on-device: "2382.12 mi away / 39 ft up" in Imperial; setting restored to metric |
| 2026-07-30 | **Design tokens + screen 1 re-skin** (design pass; iOS = reference by user decision). `design/tokens.json` → generated `DesignTokens.swift`/`.kt` (CI-checked); Android drops the Material baseline scheme for the iOS palette/surfaces and gains the component vocabulary (`app/theme/`). Scanner now mirrors iOS: stacked semantic buttons incl. a **Reverse Drift Cast top-screen entry** (was buried in the map screen), Scan + dot-status pill, Available Devices card with searching/empty states and iOS-style rows (type icon + role + signal bars, row = connect control), Virtual Rocket as the card's last row. Kept divergences: update banner (sideload-only), no Stop-scan toggle (epoch-guarded window), Roboto | iOS → Android | screens re-skin as the pass visits them; dashboard etc. still Material-styled until their turn |
| 2026-07-27 | iOS dashboard sections not yet on Android (storage bar, camera/logging controls, voice indicator) | iOS → Android | **done 2026-07-30**: StatusCard (badges + active file + BS auto-close countdown), ControlsCard (relay-aware camera/log toggles + BS CSV toggle), StorageCard (0xCC/0xCD variants, #315 auto-evict note), go/no-go ReadinessBanner — and the LOG chip dropped in the same change, re-converging the flag rows. Voice indicator had already landed with the announcer (#645). Verified live on both link types (bench OC: 22 flights / 305 MB used; bench BS: countdown 5:00, 451 MB free) |

| 2026-08-09 | **Files screen design pass** (screen-pair review, iOS canon): Android restructured to the iOS order (storage bar → header row with page caption + Select/Cancel + blue Refresh pill → progress banner with percent → empty state → list → FilePageNavigator pills → bottom selection bar); rows show iOS displayTitle dates + KB/MB sizes; StorageCard on iOS strings/capsule/legend; two delete-flow fixes (cross-page bulk delete dropped names not on the current page; BS-link dialog copy said "rocket"). Kept divergences: per-row chart/share verbs after download (iOS reaches these via Saved Flights → detail; forcing that hop on Android loses nothing but taps — kept), Android's transient status line (iOS has a CSV progress banner; Android converts synchronously), Material checkbox in selection mode (platform-native control), and **Select + the bulk-Delete pill are disabled while a download is active** (iOS leaves both live — defensive: bulk-deleting the file currently in transfer is not a state worth supporting). NOT ported: the "rocket powered off" Files empty state from the 2026-08-01 parked list — repo-wide grep shows no such state ever existed in Android; the parked item was a mistaken note, dropped | iOS → Android | **bench-verified 2026-08-10** on TR-R-e424 (direct link, FC powered): Rocket Storage card (3 flights · 3 MB used / 9 MB reserved / 509 MB free), date-titled rows with the raw-filename fallback, count caption, Refresh pill |

| 2026-08-09 | **Settings screen content pass** (screen-pair review, iOS canon — decision-independent items only): pyro channels rebuilt in the iOS form (per-channel sections, "Time after apogee"/"Altitude on descent" picker, unit-aware Delay/Altitude field storing canonical meters #160, explainer captions, iOS triple-write incl. the new `DeviceSession.mirrorPyroConfig` so dashboard tiles update without a readback, and PyroChannelTestControls twin with CONT/NO CONT/TESTING + Test Continuity); raw-int fields replaced by iOS pickers: Camera (None/GoPro/RunCam + hints), IMU Mounting (Manual/Pad auto-detect, nose axis + fin clocking, saved-code stash, "Active on rocket" readback row), IMU Logging Rate (Dynamic/1k/2k/4k + captions); Recovery gains the missing Drag k field + both iOS captions. NOT ported (still open): "Test Pyro Channel" test-fire (Ground Test screen, own pass), the device-settings sections (Link Mode / LoRa Frequency / TX Power / Network / Firmware / Calibration) pending the **parked structural decision** — iOS unifies device + profile settings in one view, Android splits (profile-only Settings; name/network in Device Manager; txPower/link-mode have NO Android surface at all today) — and the full design-language re-skin (screen is functional-Material; Tr* components not yet applied) | iOS → Android | **bench-verified 2026-08-10**: pickers show live values (IMU rate 2k, Camera RunCam, Manual mounting +X / 0°), "Active on rocket +X (manual)" appears once the FC reports, Test Continuity runs the real round trip. Two display bugs the screenshots caught and fixed: "Dynamic" chip wrapped mid-word; Drag k rendered "5.0E-4" instead of "0.0005" |

| 2026-08-10 | **Phone-fix push (cmd 47)** — Android had `SET_PHONE_FIX_BS = 47` in the command table and no codec or sender, so a launch flown with the Android phone on the base station wrote a log with no base-station position in it (the BS has no GNSS; the phone IS its position, and #723 added the firmware event row for exactly this). Ported `PhoneFixCodec` + `Commands.setPhoneFix` + the iOS throttle (30 s / 25 m) and gave `PhoneLocationManager` the horizontal-accuracy channel it never had. Six codec tests ported case-for-case from `PhoneFixCodecTests`. **Not yet verified against a real base station** | iOS → Android | code + unit tests; bench pending |
| 2026-08-10 | **Map opens on the phone** when the rocket has no latched fix (both platforms, same change): Android was showing the whole planet at zoom 0, iOS a 1 km region around a hardcoded Apple Park default. Rocket fix still wins and still takes the camera when it arrives later. Bench-verified on the Pixel | both | done |
| 2026-08-10 | **Map basemap selector design pass** (bench report on a Pixel 8, iOS canon). Android's selector was a cycling `OutlinedButton` — blue label in a thin outline drawn straight onto the imagery, advancing one source per tap — so four of five sources were invisible and the newly released Google Satellite basemap read as missing entirely after two taps. Now the iOS treatment: a 44dp layers glyph on a plate opening a menu that lists every source with the active one checked and its glyph, plus "Manage offline maps…" in the same menu (iOS's placement — the duplicate bottom-right button went away). The **active source name moved to the bottom-left attribution plate**, iOS's long-standing home for it, which is what freed the top control to be a glyph. Readability is fixed structurally rather than per-control: every floating thing on the screen (staleness badge, source button, recenter, attribution, Drift Cast) now sits on the token `card` plate — MapScreen went from importing zero design tokens to hardcoding no color but the prediction badge's staleness grade. Recenter became iOS's `location.fill` glyph in blue via a new `mapControl` token role. The Save Area screen's twin cycling chip became the same picker, over the cacheable sources only. **Android → iOS in the same pass**: the "(online)" marker Android showed on its button became `TileSource.pickerLabel` on both platforms, so every source list and both active-source plates mark what a saved area cannot cover — iOS gained it on Apple Standard/Hybrid, which had no such marker anywhere. Kept divergences: Android hides Recenter while follow is live (iOS always shows it); Android filters Google Satellite out of the list in keyless builds; Compose has no live-blur, so the plate is the opaque systemGray6 fill rather than iOS's `.ultraThinMaterial` — the stronger choice over pale imagery anyway. Since resolved: `tokens.json` gained a `yellow` palette entry on 2026-08-11 and the badge moved to `statusWarn`/`statusOk`/`statusMarginal`/`statusBad` on 2026-08-12 — it had been rendering Material Amber 700 and Yellow 700, ~12° of hue apart, so the four-rung ladder read as three | both | **bench-verified 2026-08-10 on the Pixel 8** (virtual rocket, over the same pale desert imagery as the report): menu lists all five with USGS Imagery + Topo checked in blue and "(online)" on Google Satellite and OpenFreeMap; selecting USGS Imagery swaps the style and updates the plate; Google Satellite's plate reads "Google Satellite (online) / © Google"; Recenter appears on the plate below after a pan. iOS suite 349/349 |

## Test-coverage parity

| Since | Item | Direction | Status |
|---|---|---|---|
| 2026-07-28 | OTA timing contract — shared golden `tests_cpp/fixtures/app_behavior/ota_timeouts.json`, both suites assert it plus the crossesRelay invariant | both | done |
| 2026-07-28 | `OTASession` flow tests. Android has 11 (`OtaSessionTest`) against a scripted peripheral; iOS has none, because `OTASession` reaches concrete `BLEFleet`/`BLEDevice`. Needs a protocol over the 9 members it touches + the fleet lookup behind a closure — the Android design. | Android → iOS | **done 2026-07-30**: `OTALink` protocol (BLEDevice conforms retroactively, zero changes) + `linkLookup` closure init; 9 flow cases ported (`OTASessionFlowTests`) running the REAL timeout table at 1/200 time-scale — full flow incl. the reconnect-swap property, rollback, mid-pump verify-fail, all three timeout branches incl. the FC-window-outlasts-local invariant, size gate, cancel-aborts. Wire-byte and conn-priority assertions stay Android-side by design (encoding is golden-pinned in BLEDevice; iOS has no priority boost). 304/304 suite green |
| 2026-07-29 | Announcer flight-profile tests. The #643 port put callout policy behind an `AnnouncerSpeech` seam, so Android has 15 state-machine cases iOS could not run — its policy was welded to `AVSpeechSynthesizer`. | Android → iOS | **done 2026-07-30**: iOS `FlightAnnouncer` split into policy + `SystemSpeech` engine behind the same `AnnouncerSpeech` shape as Android (isBusy/speak/stop + session status), with injectable clock and unit-system; 17 policy cases ported (`FlightAnnouncerPolicyTests`) — burnout counter incl. jitter tolerance + speed floor, 5 s altitude cadence + deadband, apogee edge + unknown-alt, first-descent-at-5 s-then-10 s, landed haversine + state fallback, STALE gate, disabled tracking, busy-skip vs interrupt, PRELAUNCH reset, enable/disable lifecycle, imperial. Dispatch (#138) + wording (#235) tests unchanged. 321/321 suite. The #390 non-focused-silence case remains Android-only (it lives in dispatch, where iOS's relay path differs structurally) |
