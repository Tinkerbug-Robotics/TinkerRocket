# Android Parity Ledger

The running divergence record between the iOS and Android apps during (and
after) the port — android-port plan §2.3. Two kinds of entries:

- **Feature lag** — a capability one app has and the other doesn't yet.
- **Behavior divergence** — twin logic that intentionally or accidentally
  differs (a bugfix landed on one side, a policy change, a platform quirk).

Rules: any change under `TinkerRocketApp/TinkerRocketApp/Models/` or
`TinkerRocketAndroid/core/` that alters shared behavior must either touch the
twin in the same PR or add a dated entry here. Entries are removed when parity
is restored. "Parity at v1.0" is checkable ⇔ this file is honest.

## Feature lag (Android behind iOS — the port burn-down)

| Since | Area | Status |
|---|---|---|
| 2026-07-23 | `:core:protocol` COMPLETE — decoders, encoders, converter, telemetry, CSV pipeline all golden-pinned (bin→CSV byte-identical to iOS) | Phase 1 done |
| 2026-07-23 | Everything above the protocol layer: `:core:session` device/fleet logic, `:core:ble`, all UI | Phases 2–9 of docs/plans/624-android-port.md |
| 2026-08-16 | **Stand-back pyro test over LoRa** is iOS-only: on a base-station link, `sendPyroContTest`/`sendPyroFire` wrap in relay cmd 50 (inner 35/36 → new uplink handlers on OC + mini), gated on `pyroCommandPathReady` (focused rocket + relay ≤ 3 s fresh); continuity on the relay path reads the SH pyro scorecard (the LoRa downlink has no pyro_status byte); BS Settings gains a "Pyro Test (via LoRa)" section opening the slow-mo PyroTestView, which re-tests continuity post-fire for a stand-back fired/not-fired verdict. Android's `PyroTestControls` never ported the fire button at all (its own safety-sensitive pass, above) — port the LoRa path together with that pass, reusing the same uplink cmds and the SH-scorecard continuity read | iOS-first |
| 2026-08-17 | **Measured-continuity scorecard bits** (`SH_PYRO_MEAS_SHIFT` = {24,26,28,30}, previously reserved) are decoded on BOTH platforms — `TelemetryData.pyroMeasuredContinuity` exists in `:core:protocol` and in iOS — but only iOS renders them, in the pyro cards and the LoRa test. Bench-found: the pre-existing `SH_PYRO_SHIFT` bits are config-gated (`if (pyroChEnabled(i))`) while the direct-BLE `pyro_status` CONT bit is not, so a ground test on a channel not armed for flight read CONT over Bluetooth and "no reading" over LoRa. The gate could not simply be dropped — any `.bad` pyro is a hard "Do not fly" in `flightReadiness` on both platforms, so a bench-tested empty channel would ground the rocket. Hence a second, ungated field; the gated bits keep their go/no-go meaning untouched. **Android now renders them on BOTH link types** (#838 item 7, 2026-08-26): the dashboard's pyro card was wrapped in `if (!isBaseStation)` under a comment claiming iOS hid it on BS links, which was backwards — so an Android operator had no continuity readout on the only link available at the pad. The card, the four-state badge and the relay continuity resolution are now at parity. **Corrected 2026-09-11 (#1048):** that claim over-stated the DIRECT-link case. Android's direct-link fallback — the path taken until some channel reports `SH_PYRO_MEAS`, which is every session's starting state — resolved a clear `pyro_status` CONT bit to `OPEN` and drew a confident red "NO CONT", where iOS resolved the identical frame to `.untested`. A clear bit cannot tell "never measured" from "measured open" (the FC sets it as `cont_known && cont_state`), so the red was a claim nobody had measured. Android now returns `UNTESTED` there too. What remains iOS-only is the stand-back LoRa TEST (row above): Android shows the reading, but cannot initiate one over the relay | reading: both; test: iOS-first |

| 2026-09-09 | **Drift Cast aim-point send + guided-flight setup** is iOS-only by deferral (#1097): Android carries `Commands.guidancePoint`, `DeviceSession.sendGuidancePointConfirming` and a semantics-exact `GuidanceSendFlow`, with no screen and no caller of the relay wrapper. The port is one unit — the guidance-law/target controls in Settings plus a send control in Drift Cast driving the existing confirming send (docs/plans/624-android-port.md item 61; #1072 covers the settings half). | deferred, tracked in #1097 |

| 2026-09-09 | **Sim banner + Stop, Ground Test, and deleting a downloaded flight** are on Android now (#1061 #1084 #1080). The sim bit was decoded and never read, with no sender for `SIM_STOP` anywhere — a simulated flight looked identical to a real one and could only be ended by power-cycling the rocket; the banner is gated on `simLaunched || telemetry.simActive` (iOS's #393 rule, so it survives a reconnect) and its Stop is relay-aware. The sim latch also moved above the relayed-path `return`, so it advances on base-station links at all. Ground Test (cmds 15/16) had ids and nothing else: it is a direct-link-only control with a banner carrying its own Stop, since the FC's mix has no idle or link-loss timeout and its only failsafe is launch detection. Saved Flights gained a per-row Delete (`.bin` + `.csv` + summary sidecar, confirm dialog worded "from this phone"): downloads live in durable storage on both platforms by design, so nothing reclaimed the space and Settings → Clear storage was the only remedy. Still iOS-only: the Ground Test SCREEN (attitude readout and per-channel test fire) — this is the dashboard control and banner only. | iOS → Android | done |
| 2026-09-09 | **Offline-region snapshot (dc1bc29, iOS) ported, and the OTA screen renders its terminal states** (#1045 #1063). iOS hoisted the download spec and source into locals and disabled the Save Area controls for the run in dc1bc29 — shared behaviour that never got an Android twin OR a ledger row, so the divergence was unrecorded as well as unported: Android wrote the manifest from live Compose state when the run finished minutes later, so a slider, the source picker or the map centre moved mid-download produced a saved region describing tiles that were never fetched. Android now snapshots all of it, freezes the sliders and the source picker while `phase == DOWNLOADING`, and stops the camera-idle listener rewriting the centre. Separately, Android's device-less OTA screen chose its branch from a non-snapshot map, so a failed flash kept "Firmware update in progress" with a spinner and "Working…" — the reason `OtaSession` had written, which names the remedy, never reached the operator. The branch now observes the session's state and Failed/Verified/RollbackDetected each render, with a Dismiss button and a back handler. | iOS → Android | done |

| 2026-09-09 | **Guidance settings and the sensor-cal import** (#1072, #1059 first half). Android's Guidance section was a bare enable toggle plus five fields: no LAW selector, no station-keep kp/kd, no Accel→Fin — so station-keeping was reachable only by adopting a rocket already configured that way, and once adopted its two gains could not be edited, while the syncer had always SENT every one of those fields from the profile. Ported with iOS's shape: law picker, per-law tuning (the other law's values stay in the profile and keep going out, so switching never zeroes its tune), and the shared fields. Roll control and Guidance also became one CONTROL MODE selector, as on iOS: they are exclusive in the firmware (guidance rate-nulls roll in every phase), and two independent toggles let the operator ask for a flight the rocket will not fly; switching to Guidance stashes the roll sub-mode and restores it on the way back. `importRocketSensorCalIntoActiveProfile` is the mag import's twin, so the sensor-cal card has an Import button — it used to render "the rocket has a calibration this profile doesn't (run calibration to save one)" with no button and no way to run one. Still iOS-only: the on-pad sensor-cal RUN (cmd 21 + a ~12 s busy state + the 9.80665 magnitude check), tracked in #1059. | iOS → Android | done |

| 2026-09-09 | **Onboarding + first-connect provisioning** (#1057). iOS gates the dashboard behind an onboarding screen that stores a network name and its derived non-zero ID, then auto-raises a provisioning sheet the first time a device reports an identity it has never set up. Android had neither: `AppNetworkStore` started at name `""` / ID **0**, and `KnownDeviceStore.isProvisioned` / `markProvisioned` had no callers outside their unit tests. The consequence is narrower than "Android fleets are broken" and worth stating exactly: the firmware's factory default is ALSO 0, so an all-factory Android fleet is internally consistent and telemetry flows — the break is a MIXED fleet (a rocket provisioned from iOS onto a non-zero ID, then a base station set up from the Android phone: Android pushes nothing, the BS stays at 0, and the two stop matching), and every mismatch surface in Device Manager is gated off while the app's ID is 0, so nothing says why. Both halves are ported, with the same FNV-1a-derived ID and live preview; Device Manager remains the edit surface, and forgetting a device there now really does make it "treated as new the next time it connects" — which that screen has always promised. | iOS → Android | done |
| 2026-09-09 | **Map camera follow and the rocket marker's own facts** (#1068), a divergence pair the ledger recorded neither half of. iOS centred on the first rocket fix, latched `hasInitializedRegion` and then never moved the camera again for the rest of the visit — a rocket drifting under canopy walked off the screen and only the Recenter button brought it back, once per drift; it now follows the fix (pan only, keeping the operator's zoom), a pan stops the follow, and Recenter resumes it. Android has followed with a gesture break since its map shipped and is unchanged. The other way: iOS's marker opens a callout reading "N sats • Ns ago", where Android's was a bare 9 px circle with no callout, click listener or text layer — a marker frozen on a four-minute-old fix looked exactly like a live one, on the screen used to walk to a rocket. Android now shows the same two facts on a plate under the prediction badge, on the same 1 Hz tick and with the same staleness grading. **Deliberate divergence:** a plate that is always visible rather than a callout on tap — a MapLibre SymbolLayer needs a sprite and a font stack for one line of text, and the facts matter more than the gesture. | both | done |

## Behavior divergences

| Since | Divergence | Rationale |
|---|---|---|
| 2026-09-09 | **Firmware image validation before flashing (#773)** ships on both, with one gap: iOS also compares the picked image's board suffix against the FC's own reported version when the target is the Flight Computer; Android skips that check for the FC target. | Android's `DeviceSession.identity` carries a single `firmwareVersion` — the BLE peer's, i.e. the out computer's — with no equivalent of iOS's separate `fcFirmwareVersion` (fed by the `fc_fw` identity key). Comparing the OC's version against an FC image would produce a false warning on every FC update, so the check is omitted rather than wrong. The refusal that matters — wrong `project_name` — is identical on both, and the chip check is identical for the OC and base station. Closing the gap means plumbing `fc_fw` into the Android identity. |
| 2026-09-09 | **Landing predictor + Drift Cast lifecycle (#1044 #1051 #1056 #1055 #1075)**: Android's wind prefetch hops to `Dispatchers.IO` (it blocked the single `tr-fleet` thread — every BLE decode, callout and command write — for up to 30 s per attempt, once per frame while failing); both predictors fetch one profile at a time and back off 45 s after a failure (both refetched on every preflight frame until the first success); Android's predictor is owned by the container and `attach()` keeps the pinned prediction and cached wind across reconnects and tab switches (`reset()` only for a different device — iOS semantics; a drop mid-descent used to fly the rest of the flight with zero wind); Drift Cast coordinates are written/read locale-independently on Android (`Locale.ROOT`, `%.6f` like iOS; decimal-comma installs recover on read); iOS no longer overwrites the saved landing target from the phone fix on every open (Android is the reference). | both | done |
| 2026-09-09 | **Chart/trajectory CSV parse streams and caps at 100 Hz on BOTH apps** (#1081). Android's `CsvParser.parse(lines, maxSampleHz)` landed in b6457c9 (#636) with no entry here; iOS kept reading the whole file into `[[Double]]` reserved at the full row count. `CSVParser.parse(url:maxSampleHz:)` now twins it, with `parse(url:)` still full-rate. | Recorded retroactively — the divergence existed unrecorded from #636 until this row; parity restored, row kept as the record. |
| 2026-09-08 | **Mag-cal teardown abort** now exists on both. Android has aborted on every teardown since its first version (`MagCalScreen.kt` `DisposableEffect { onDispose { … } }` plus a `BackHandler`, gated on a remembered `needsAbort` rather than live status); iOS sent `MAG_CAL_ABORT` only from three deliberate taps and had no teardown hook at all, so a swipe-down on the hosting Settings sheet destroyed the view without telling the rocket. iOS gained an `.onDisappear` with the same remembered-flag gating (#1037). | Android's shape was correct and iOS adopted it. Gating on a remembered flag rather than live `status` is load-bearing on both: the sub-state comes from 5 Hz FC telemetry and can be nil or stale at exactly the moment teardown happens. The stranded states are SAMPLING and REVIEW, which have no auto-exit; VERIFYING self-resolves via the FC's 60 s timer but is included because the flag cannot always tell them apart at teardown. |
| 2026-08-26 | **Android's Path views draw BOTH position solutions** — the EKF nav-filter track and the GNSS track, labelled, with a second landing marker when they differ. iOS draws the GNSS track only. | #741 measured the two solutions landing **81 m apart** on the CENJARS flight, and set the rule: draw both and say so, never average or silently prefer one. Android could adopt it directly because its Path view is an abstract local-metre canvas — the GNSS track just converts to ENU. iOS draws over georeferenced imagery, so showing the EKF track there needs the filter's ENU origin, which the firmware writes to no frame and no sidecar (#741) and which must be reconstructed by inverting the firmware's own forward conversion. That port is the remaining work; until it lands iOS shows one solution and Android shows two. |
| 2026-08-25 | Both apps decode and render the LoRa drop counters `nidd` and `szd`, but iOS escalates the **nidd** line from informational to a warning when the base station is hearing no rocket (`trackingHealthy`); Android renders it informational always. `szd` is a warning on both. | Another BS/rocket pair on its own network id is normal traffic, so nidd is only a fault when it comes with no rocket — Android's dashboard has no tracking-health signal to gate on, and inventing one was out of scope for #838 item 4. `szd` has no benign reading on either platform. |
| 2026-07-23 | Download API returns typed `DownloadResult.Busy` for a second concurrent download; iOS silently overwrites the in-flight completion handler (leaking it) | Silent handler loss is a defect; byte/wire behavior unchanged |
| 2026-07-23 | Reconnect endgame is **re-scan → sighting-gated autoConnect**; iOS parks a CoreBluetooth `connect()` forever | Android `autoConnect=true` from a bare MAC can silently never fire (plan §3); the CB parked connect has no analog |
| 2026-07-27 | FreqScan's fixed-frequency **Apply** (transactional relay + verify + rollback) is iOS-only; Android shows a "use iOS" note | The fleet runs hopping mode (#150 default) where Apply is refused by design — the mask auto-applies. Port the transactional flow when a fixed-frequency need reappears |
| 2026-07-23 | Scan restart within 15 s is NOT truncated (epoch-guarded timeout); iOS's unguarded timer truncates a restarted scan | The iOS behavior is a timer-hygiene bug; a restarted scan deserves its full window |
| 2026-07-23 | KnownDeviceStore device list sorts plain case-insensitive; iOS uses `localizedCaseInsensitiveCompare` | Locale-dependent ordering is cosmetic; plain ordering is deterministic across devices |
| 2026-07-30 | Virtual Rocket is on BOTH platforms (Android `DemoMode.kt`, iOS `VirtualRocket.swift` — same FLIGHT SCRIPT, frame for frame; the cmd-20 config readback is now identical too (#1100, 2026-09-11: both emit config + config_identity, neither emits config_pyro — matching sendCurrentConfig); one flight per SIM_START via the standard Simulation UI, SIM_STOP honored). iOS runs it in the Simulator (no CoreBluetooth). Divergence: the pre-staged downloadable sample flight is Android-only — iOS has no transport fake to serve file ops | Files-in-virtual: Android-only | Android's demo rides FakeFirmware (a full transport); iOS's rides a command tap + direct demux feed, which covers everything except the file pipeline. Follow-up noted: suppress the provisioning sheet for the virtual device (registry hygiene) |
| 2026-07-30 | In-app update checker (GitHub Releases → scanner banner → browser) is Android-only | Distribution differs by design (plan §1): iOS updates ride the App Store; Android sideloads from GitHub Releases, so the app itself must notice a new `android-v*` tag. Pure logic in `UpdateCheck.kt` (JVM-tested); fetch is throttled to one hit/day and silent on every failure — no signal at a launch site is normal |
| 2026-09-11 | GNSS-absent degraded-flight banner (`sensor_health` bit 22, #557) | iOS → Android | **done 2026-09-11 (#1082)** — decoded into `gnssAbsentMode` since forever and consumed by nothing on Android; the FC boot line is not a fallback surface (suppressed before READY on a direct link, never sent over a relay). `GnssAbsentBanner` now sits in the iOS slot, after the state banner |
| 2026-09-11 | Relay `lastSeen` is stamped only from a LIVE frame | both | **done 2026-09-11 (#1036)** — the base station re-pushes the focused rocket's cached frame every 2 s tagged STALE, and both apps stamped `lastSeen` off it, so every "is the rocket still there" age stayed bounded by 2 s forever. Guarded identically in `RemoteRocket.updateTelemetry` and `DeviceSession`; the telemetry assignment is unchanged so cached values still render |
| 2026-09-11 | Base-station storage card admits the UNMOUNTED frame | both | **done 2026-09-11 (#1083)** — the same dead branch ported twice: firmware reports `total == 0` exactly when the mounted bit is clear, so gating on `total > 0` skipped the card in the one state its "not mounted" warning exists for. Bar and legend are suppressed while unmounted |
| 2026-09-11 | Settings is read-only over a base-station link | iOS → Android | **done 2026-09-11 (#1038)** — Android rendered the full editor; every edit persisted to the profile and `pushGroup` returned early without sending, while the dashboard's pyro card read the un-sent value back out of that same profile and rendered it as confirmed. Now iOS's read-only Active Rocket + Summary |

## Feature lag (session layer, deferred to Phase 3)

| Since | Area | Status |
|---|---|---|
| ~~2026-07-23~~ | ~~RocketRoster merge + the 10 RocketRosterTests cases~~ | **PORTED 2026-07-27** — RocketRoster.kt + all 10 cases green; single-BS relay UI live (multi-device fleet chrome remains Phase 9) |

## UI parity (both directions — see docs/design-language.md)

| Since | Item | Direction | Status |
|---|---|---|---|
| 2026-08-12 | **78 hardcoded Material colors converted to design tokens** across 12 Android screens. Not a tidy-up: the literals were Material Design values (#43A047 Green 600, #B00020 error, #1E88E5 Blue 600…) that had never been converted, so Material greens rendered beside iOS-palette ones on the same card. 12 roles added (`accent`, the `statusOk/Marginal/Warn/Bad/Pending/Active` ladder, `pyroFired`, `deviceRocket`/`deviceBaseStation`, `launchSite`/`landingSite`), replacing what would have been nine per-screen triads for one meaning. **Twelve real divergences fixed**, each Android signalling a different meaning than iOS: the profile sync ladder was inverted (gray on awaiting-sync where iOS is orange — Android's own comment cites #375, the fix the color cancelled); the base-station badge was teal, which means Ground Test in this system, where iOS is orange; an infeasible Drift Cast read as merely degraded rather than red; the caution rung was Material Yellow where iOS is orange, at three sites; the mag-cal chip-limit warning sat on the explanatory paragraph while the number actually near the limit carried no color; and the verify gates rendered **green before any sample arrived** (`pass: Boolean` with `x == null || …`) — now tri-state. Deliberate keeps: the shared lit-green flag chip (#2E7D32, hardcoded identically on both platforms on purpose), the six-color chart series palette (categorical, not status), and the ten 3D scene literals (exact round-trips of iOS's SceneKit UIColors, none of which is a palette entry — tokenising them would *create* a divergence). Two solid fills became tinted (staleness banner, sync pill): white-on-solid was legible against Material 700/800 but drops to ~2.2:1 on the lighter palette  **Second population, same defect class:** 19 more Material hexes lived in MapLibre *string* form (`lineColor("#43A047")`), which the `Color(0x…)` audit did not match — MapScreen was briefly half-converted, its prediction badge on `statusOk` while its landing circle stayed Material green. Converted via the `@ColorInt` overload (`lineColor(tr.landingSite.toArgb())`). That pass found the **Drift Cast overlay scheme rotated on every element**: Android drew launch blue / landing RED / guidance purple with the two lines carrying each other's dash pattern, where iOS draws launch red / landing green / guidance blue, boost dashed blue, descent solid purple. A red landing marker is iOS's launch-pad colour — with two phones out at a launch that is a recovery hazard, the same one FlightTrajectoryScreen records fixing. Style installs are now keyed on the theme so overlays repaint on a light/dark flip; Drift Cast's effect had to be **split** rather than re-keyed, because MapLibre cannot unregister a click listener and a theme flip would have stacked one per flip. Kept literal: the white marker rings (chrome against imagery, which does not theme) and `#ededed` (iOS's placeholder substrate, `CachingTileOverlay.swift:27`) | iOS → Android | **code + build green; bench pending** |
| 2026-07-27 | Flight-event flag chips (LAUNCH/BURNOUT/APOGEE/LANDED/LOG illuminating) | Android → iOS | **done 2026-07-30** (`FlightEventFlagsView`, render-tested) |
| 2026-07-27 | Sensor-health dot row (per-subsystem green/amber/red/gray dots) | Android → iOS | **done 2026-07-30** (`HealthDotRow` replaces the labeled grid). The go/no-go banner completed the round trip the same day — Android now renders `flightReadiness` too (`ReadinessBanner`), so the health cards match in both directions. **Corrected 2026-09-11 (#1039):** they did not match — iOS `e70508a` (#836 item 3) had added a staleness hold that Android never got, so a dead flight computer kept rendering a green "Ready to fly" at full strength while the red STALE banner sat four cards above it. Android now holds the verdict too (`ReadinessHold` in `:core:protocol`, shared rule + JVM tests) and dims the card |
| 2026-07-31 | **Dashboard missing-section fix** (user side-by-side review caught it): the reorder had matched order but not CONTENT — Android was missing the Signal bars card, the Battery table (charge/voltage/current), the whole IMU card, and the GPS row. All four ported; summary placement now follows iOS's per-link split (BS: after banners; direct: after IMU); pyro card now direct-links-only ("Pyro Channels" 2×2, iOS form); Start Logging orange. Remaining gaps: Ground Test screen (PyroTestView port — safety-sensitive, own pass), virtual-rocket script doesn't feed IMU triplets (demo polish). ~~per-channel "Disabled"/trigger text + Test Continuity, direct-link IMU nose annotation~~ **CLEARED 2026-08-09** — the "0xB1 config readback" label was a misnomer (0xB1 is the OC↔FC I2C `PID_CONFIG_MSG`, unrelated): the cmd-20 JSON readback (`config_pyro` / `imu_orient`) was already parsed and folded by `:core:session`; only dashboard wiring was missing. Landed as the iOS tile form (FIRED/CONT/NO CONT badge ladder, TESTING spinner during the 2.5 s cmd-35 window, trigger text via `UnitFormatter`), nose annotation with mode label + off-axis orange on both link types, High-G row hidden on BS links, and Imperial g-conversion for acceleration rows (two extra IMU-card parity misses caught in passing). Note: none of this renders in demo mode — the virtual device is a base station and the pyro card is direct-link-only | iOS → Android | verified against the user's iPhone screenshots via virtual rocket |
| 2026-07-31 | **Dashboard section order settled**: iOS layout is canon (user decision), Android reordered to it — flight summary right under power, Status before the flag chips, tools last, storage bar moved to the Files screen (its iOS home). The ONE deliberate exception: Android keeps the full dashboard with the power section near the top in the OFF state (iOS swaps to a battery+PowerOn-only view). Direct-link summary placement follows the BS-link position (iOS splits them; the flown-with BS layout wins for consistency) | iOS → Android (+doc) | design-language.md updated; iOS unchanged |
| 2026-07-30 | **Connected chrome + dashboard re-skin** (design pass, overnight batch): iOS toolbar arrangement replaces the text-tab strip (chevron + units menu leading; book/map/voice-speaker/gear trailing; chevron = back-to-dashboard from tabs, disconnect from dashboard — iOS root-chevron double duty); voice becomes the speaker icon with session-health tint (long-press test). Dashboard: iOS `RocketStateView` twin lands **with the #382 READY→PRELAUNCH display mapping Android never had**, plus the `FlightSummaryView` Current/Max table (altitude/speed/attitude) and iOS semantic colors on the Tools row. Held for morning review: dashboard section ORDER (iOS's own order differs from the design-language settled order Android follows), hop badges on the banner, Files/Settings depth | iOS → Android | verified on-device (virtual rocket, both halves screenshotted) |
| 2026-07-30 | **Display units + branding** (iOS #160 twin): global Metric/Imperial via the scanner's ruler menu (iOS toolbar mirror), persisted under the same key/values (`unitSystem`: metric/imperial), `UnitFormatter` ported format-string for format-string into `:core:session` (pinned by `UnitFormatterTest`), announcer speaks the setting, dashboard live values convert (alt cards, bearing distance/up-down). Tinkerbug logo tops the scanner right, same asset as iOS. Chart axes + planning tools stay SI until their design-pass turn | iOS → Android | verified on-device: "2382.12 mi away / 39 ft up" in Imperial; setting restored to metric |
| 2026-07-30 | **Design tokens + screen 1 re-skin** (design pass; iOS = reference by user decision). `design/tokens.json` → generated `DesignTokens.swift`/`.kt` (CI-checked); Android drops the Material baseline scheme for the iOS palette/surfaces and gains the component vocabulary (`app/theme/`). Scanner now mirrors iOS: stacked semantic buttons incl. a **Reverse Drift Cast top-screen entry** (was buried in the map screen), Scan + dot-status pill, Available Devices card with searching/empty states and iOS-style rows (type icon + role + signal bars, row = connect control), Virtual Rocket as the card's last row. Kept divergences: update banner (sideload-only), no Stop-scan toggle (epoch-guarded window), Roboto | iOS → Android | screens re-skin as the pass visits them; dashboard etc. still Material-styled until their turn |
| 2026-07-27 | iOS dashboard sections not yet on Android (storage bar, camera/logging controls, voice indicator) | iOS → Android | **done 2026-07-30**: StatusCard (badges + active file + BS auto-close countdown), ControlsCard (relay-aware camera/log toggles + BS CSV toggle), StorageCard (0xCC/0xCD variants, #315 auto-evict note), go/no-go ReadinessBanner — and the LOG chip dropped in the same change, re-converging the flag rows. Voice indicator had already landed with the announcer (#645). Verified live on both link types (bench OC: 22 flights / 305 MB used; bench BS: countdown 5:00, 451 MB free) |

| 2026-08-09 | **Files screen design pass** (screen-pair review, iOS canon): Android restructured to the iOS order (storage bar → header row with page caption + Select/Cancel + blue Refresh pill → progress banner with percent → empty state → list → FilePageNavigator pills → bottom selection bar); rows show iOS displayTitle dates + KB/MB sizes; StorageCard on iOS strings/capsule/legend; two delete-flow fixes (cross-page bulk delete dropped names not on the current page; BS-link dialog copy said "rocket"). Kept divergences: per-row chart/share verbs after download (iOS reaches these via Saved Flights → detail; forcing that hop on Android loses nothing but taps — kept), Android's transient status line (iOS has a CSV progress banner; Android converts synchronously), Material checkbox in selection mode (platform-native control), and **Select + the bulk-Delete pill are disabled while a download is active** (iOS leaves both live — defensive: bulk-deleting the file currently in transfer is not a state worth supporting). NOT ported: the "rocket powered off" Files empty state from the 2026-08-01 parked list — repo-wide grep shows no such state ever existed in Android; the parked item was a mistaken note, dropped | iOS → Android | **bench-verified 2026-08-10** on TR-R-e424 (direct link, FC powered): Rocket Storage card (3 flights · 3 MB used / 9 MB reserved / 509 MB free), date-titled rows with the raw-filename fallback, count caption, Refresh pill |

| 2026-08-09 | **Settings screen content pass** (screen-pair review, iOS canon — decision-independent items only): pyro channels rebuilt in the iOS form (per-channel sections, "Time after apogee"/"Altitude on descent" picker, unit-aware Delay/Altitude field storing canonical meters #160, explainer captions, iOS triple-write incl. the new `DeviceSession.mirrorPyroConfig` so dashboard tiles update without a readback, and PyroChannelTestControls twin with CONT/NO CONT/TESTING + Test Continuity); raw-int fields replaced by iOS pickers: Camera (None/GoPro/RunCam + hints), IMU Mounting (Manual/Pad auto-detect, nose axis + fin clocking, saved-code stash, "Active on rocket" readback row), IMU Logging Rate (Dynamic/1k/2k/4k + captions); Recovery gains the missing Drag k field + both iOS captions. NOT ported (still open): "Test Pyro Channel" test-fire (Ground Test screen, own pass), the device-settings sections (Link Mode / LoRa Frequency / TX Power / Network / Firmware / Calibration) pending the **parked structural decision** — iOS unifies device + profile settings in one view, Android splits (profile-only Settings; name/network in Device Manager; txPower/link-mode have NO Android surface at all today) — and the full design-language re-skin (screen is functional-Material; Tr* components not yet applied) | iOS → Android | **bench-verified 2026-08-10**: pickers show live values (IMU rate 2k, Camera RunCam, Manual mounting +X / 0°), "Active on rocket +X (manual)" appears once the FC reports, Test Continuity runs the real round trip. Two display bugs the screenshots caught and fixed: "Dynamic" chip wrapped mid-word; Drag k rendered "5.0E-4" instead of "0.0005" |

| 2026-08-10 | **Phone-fix push (cmd 47)** — Android had `SET_PHONE_FIX_BS = 47` in the command table and no codec or sender, so a launch flown with the Android phone on the base station wrote a log with no base-station position in it (the BS has no GNSS; the phone IS its position, and #723 added the firmware event row for exactly this). Ported `PhoneFixCodec` + `Commands.setPhoneFix` + the iOS throttle (30 s / 25 m) and gave `PhoneLocationManager` the horizontal-accuracy channel it never had. Six codec tests ported case-for-case from `PhoneFixCodecTests`. **Not yet verified against a real base station** | iOS → Android | code + unit tests; bench pending |
| 2026-08-10 | **Map opens on the phone** when the rocket has no latched fix (both platforms, same change): Android was showing the whole planet at zoom 0, iOS a 1 km region around a hardcoded Apple Park default. Rocket fix still wins and still takes the camera when it arrives later. Bench-verified on the Pixel | both | done |
| 2026-08-10 | **Map basemap selector design pass** (bench report on a Pixel 8, iOS canon). Android's selector was a cycling `OutlinedButton` — blue label in a thin outline drawn straight onto the imagery, advancing one source per tap — so four of five sources were invisible and the newly released Google Satellite basemap read as missing entirely after two taps. Now the iOS treatment: a 44dp layers glyph on a plate opening a menu that lists every source with the active one checked and its glyph, plus "Manage offline maps…" in the same menu (iOS's placement — the duplicate bottom-right button went away). The **active source name moved to the bottom-left attribution plate**, iOS's long-standing home for it, which is what freed the top control to be a glyph. Readability is fixed structurally rather than per-control: every floating thing on the screen (staleness badge, source button, recenter, attribution, Drift Cast) now sits on the token `card` plate — MapScreen went from importing zero design tokens to hardcoding no color but the prediction badge's staleness grade. Recenter became iOS's `location.fill` glyph in blue via a new `mapControl` token role. The Save Area screen's twin cycling chip became the same picker, over the cacheable sources only. **Android → iOS in the same pass**: the "(online)" marker Android showed on its button became `TileSource.pickerLabel` on both platforms, so every source list and both active-source plates mark what a saved area cannot cover — iOS gained it on Apple Standard/Hybrid, which had no such marker anywhere. Kept divergences: Android hides Recenter while follow is live (iOS always shows it); Android filters Google Satellite out of the list in keyless builds; Compose has no live-blur, so the plate is the opaque systemGray6 fill rather than iOS's `.ultraThinMaterial` — the stronger choice over pale imagery anyway. Since resolved: `tokens.json` gained a `yellow` palette entry on 2026-08-11 and the badge moved to `statusWarn`/`statusOk`/`statusMarginal`/`statusBad` on 2026-08-12 — it had been rendering Material Amber 700 and Yellow 700, ~12° of hue apart, so the four-rung ladder read as three | both | **bench-verified 2026-08-10 on the Pixel 8** (virtual rocket, over the same pale desert imagery as the report): menu lists all five with USGS Imagery + Topo checked in blue and "(online)" on Google Satellite and OpenFreeMap; selecting USGS Imagery swaps the style and updates the plate; Google Satellite's plate reads "Google Satellite (online) / © Google"; Recenter appears on the plate below after a pan. iOS suite 349/349 |

## Test-coverage parity

| Since | Item | Direction | Status |
|---|---|---|---|
| 2026-09-11 | **FC-sourced pyro readback (#1231)** — the `config_pyro` frame now carries `src`: `"fc"` = the flight computer's live `pyro_config`, relayed from the v2 config report (the struct that actually fires); `"oc"` = the out computer's own cache (rail off, or a pre-#1231 FC); absent = a pre-#1231 OC. FC-sourced only, `fnv` says whether the FC's copy is a stored record — false is a board that has never been configured reporting its all-disabled default, which looks exactly like four channels somebody switched off. Both apps decode into `RocketConfig.pyroSource` / `pyroStoredOnFlightComputer` (`PyroConfigSource` on both), carry them across a `config` rebuild, gate the #1078 optimistic write mirror on "not FC-sourced, or the rail is off" (the FC's echo re-draws the tiles; a frame it never applied then leaves them on its previous values, which is the divergence made visible), and put one caption under the pyro card when the tiles are not the FC's own stored config. iOS: `PyroReadbackProvenanceTests` (parse, carry-over, mirror gate). Android: `TelemetryDispatchTest`, `DeviceSessionTest.pyroMirror_yieldsToFlightComputerEcho`, and `PyroCardPolicyTest` — the caption text lives in `:core:protocol` (`PyroCardPolicy.provenanceNote`) so it is pinned, `:app` having no test source set. | both | **bench-verified 2026-09-11** on the V9 pair, including the Android tiles on the Pixel (FC record shown over a differing OC cache; OC copy with caption after Power off) |
| 2026-09-10 | **FC-relay OTA pacing raised 12 → 20 KB/s (#811)** — bench-measured on the V9 pair with the OC console captured, not reasoned. The cap sets the transfer time (`image / cap`) and the I2S idle-fill #811 blamed is `1 − cap/50KB` falling out of it, so every I2S-side fix that issue proposed would have moved the ratio and not the clock. 12 → 53.0 s, 20 → 31.3 s, 30 → 20.7 s, 40 → 15.2 s but with the OC's 16-frame feed queue saturated and `ACL buf alloc failed` — the #627 signature, reproduced deliberately. 20 shipped rather than 30 because these are one run each on one board, one image, Android only. Both platforms' constants and the shared fixture move together, and both suites' range assertions now bound the cap by the MEASURED saturation point instead of by "iOS's observed rate". | both | done |
| 2026-09-10 | **Firmware kept on the phone, for a field with no signal (#773)** — `FirmwareCache` on both platforms: content-addressed by SHA-256 so an unchanged image across two releases is stored once, and re-hashed on EVERY read because the file name is a claim about the contents and not proof of them — anything failing is deleted rather than left to be trusted on a second look. The MANIFEST is cached too, which is what actually satisfies the acceptance line: images alone would not do it, because with no network the app cannot list anything and so cannot offer what it holds. It round-trips through the same parsers the network path uses (`toListingJson` / `toListingJSON`), so there is one codec rather than two that can disagree. An unreachable network is a FALLBACK, not a failure, and says the catalog may be stale; no signal AND nothing cached says both halves. `prefetch` is the download-at-home action, naming what it could not fetch rather than counting it, and pruning superseded images. 13 cases mirrored per platform. | both | done |
| 2026-09-10 | **The OTA finish watchdog ignored the only progress there was (#773)** — `awaitFinish` compared the reported STATE and nothing else, so the device's 2 Hz `writing` updates never reset the no-progress budget: `writing` → `writing` is not a change, even though its byte count climbs. The out-computer console settled what actually fills that window — `OTA_FINISH` at t=43.71 s and `ready to boot` at t=44.09 s on an 815,696 B image, so the blocking verify is 380 ms and the other ~26 s is the transfer still draining out of the phone's BLE stack after the pump loop returned. Both platforms now key on (state, bytes); a device repeating the same byte count still times out, which is the half that keeps it a watchdog rather than a licence to wait forever. Two cases mirrored, and the comments in `OtaTimeouts`, `OTATimeouts`, the shared fixture, the plan doc and `TR_OTA_Receiver.h` are corrected — they all described a verification cost that measurement says is not there. | both | done |
| 2026-09-10 | **The catalog recommended the mini's image to a V9 (#773)** — found on the bench against the real `fw-v0.1.0`. `FirmwareCatalog.best` returned the first candidate when no board was known, and with nothing to rank on the sort falls back to the board string, so `m1` beat `v8` and `v9` on spelling and was shown with a tick. Boards provisioned before #773 step 2 report no revision, so that is the ordinary state of existing hardware. Two halves, both mirrored: the session now falls back to the board named in the unit's RUNNING VERSION — which `EspImage.check` always used and the catalog was never given — and `best` returns null rather than guessing when the board is unknown and every candidate is board-specific, while still recommending a suffixless image (one build, applies everywhere). `Ready` carries `boardKnown` so the screens can say "this unit has not reported which board revision it is" rather than "nothing here is built for this board" — different problems, different sentences. Kotlin gained `EspImage.boardSuffix(version)`, which iOS already had. | both | done |
| 2026-09-10 | **Choosing a published image, in the app (#773 step 4c)** — `FirmwareCatalogSession` on both platforms sequences check → list → download → verify, and the screens render its state and nothing else. Nine cases mirrored, including the ones that decide what an operator is told: no network reads as a connection problem rather than missing firmware (at a launch site that is nearly always the cause, and the wrong wording sends them looking in the wrong place); a release with nothing for this unit names the release and the project; a board with no matching image still gets the list but no default, because refusing to guess is not refusing to show; and an image the unit already runs is flagged as a re-flash rather than hidden. A verified download is adopted as the picked file on both — Android sets `pickedBytes`, iOS writes a temp file — so it goes through the same `EspImage.check` as a hand-picked one and the manifest is never the last word on what gets flashed. | both | done |
| 2026-09-10 | **The OTA verify heartbeat (#773)** — the wire gained an `ota_status` `verifying` state, sent once by the device immediately before the blocking terminal work (`esp_ota_end()` re-reading the staged image, then setting the boot partition). Both apps' finish wait became a NO-PROGRESS budget instead of a total: any status the device sends restarts it, so a device that is demonstrably working is never cut off by a clock, and the two silences now report differently ("reported no progress" vs "reported verifying but did not finish"). Three cases mirrored per platform, including the compatibility one — firmware older than this sends nothing, the budget never restarts, and the wait behaves exactly as the old fixed one did. | both | done |
| 2026-09-10 | **A successful local OTA reported failure (#773)** — `OtaStage.FINISH`'s non-relay window was 15 s on both platforms, which is only the device's SHA-256 over the image plus setting the boot partition, the bytes having already arrived. Bench 2026-09-10 flashing `fw-v0.0.1-rc1`: an 815 kB out-computer image and a 770 kB base-station image were BOTH still verifying when it expired, so the app said "Device did not finalize OTA within 15s" on two flashes that committed and booted. Raised to 45 s on both platforms and in the shared fixture `tests_cpp/fixtures/app_behavior/ota_timeouts.json`, staying under the FC's 60 s so the `crossesRelay` contract still holds. Same failure #627 fixed on the FC path in July; the local path kept the old number and the images grew into it. | both | done |
| 2026-09-10 | **A refused firmware image said "Not connected" on Android (#773)** — `FirmwareUpdateScreen`'s FlashButton took one `connected` flag that the call site built as `connected && verdict !is Refuse`, so an image refused for the wrong project printed "Not connected" in red beneath a Device card reading Connected. iOS never had this: `FirmwareUpdateView` disables on `!device.isConnected` and `imageVerdict?.isRefusal` as separate terms and prints no message for either. Android now matches. Found on the bench against a live V9 at −41 dBm while testing the release path; no automated test — `:app` has no Compose test harness. | both | done |
| 2026-09-10 | **Board suffixes that are not `-v` (#773)** — `EspImage.boardSuffix` on both platforms, and `image_info.py`'s `board_of` that feeds the manifest, matched only `-v<n>`. Every `-m1` image — the flight-computer and out-computer builds for the rocket-computer-mini — therefore parsed as "no board", which the catalog reads as "applies everywhere" for the one image that applies to exactly one board. Four cases mirrored per platform: `-m1` labelled, a base-station `-v3` labelled, the mini's own `-b1` labelled, and the build-date tail (`+20260909-2043`) still not mistaken for one. The firmware side never had the bug — `TR_OTA_Receiver` strstr's the whole suffix — so the app was proposing a flash the board would then refuse. | both | done |
| 2026-09-10 | **Finding and fetching the firmware release (#773 step 4b)** — `FirmwareReleaseLocator` (which GitHub release is the firmware one) and XPLACEHOLDERX. Both suites read the SAME golden fixture, `TinkerRocketAndroid/core/protocol/src/test/resources/github_releases_golden.json` — a real capture of this repo's `/releases`, containing four releases and no firmware, which is the case that proves why `/releases/latest` must not be used: it answers a gerber zip. The refusal split is mirrored too — a failed transfer is `Unreachable` ("try again on better signal") and bad bytes are `Corrupt` ("do not flash this"), never returned either way. | both | done |
| 2026-09-10 | **Firmware manifest + catalog selection (#773 step 4)** — `FirmwareManifest.parse` and `FirmwareCatalog.forUnit` / `.best` on both platforms, 13 cases mirrored. Both suites read the SAME golden fixture, `TinkerRocketAndroid/core/protocol/src/test/resources/firmware_manifest_golden.json`, produced by `image_info.py --manifest` against images actually built from this tree — iOS locates it from `#filePath` rather than keeping a copy, so the two platforms cannot drift from each other or from what the release workflow emits. | both | done |
| 2026-09-10 | **Provisioned board revision (#773 step 2)** — `fc_board` / `oc_board` on the direct-link `fc_identity` message, folded into each app's device identity, and passed to `EspImage.check` as `provisionedBoard`, which WINS over the running version. Five cases mirrored: the wrongly-flashed-board scenario the feature exists for, agreement staying silent, the fallback saying it is a fallback, an empty string reading as unprovisioned rather than as a board named "", and an image with no board suffix never being board-warned. | both | done |
| 2026-09-09 | **LoRa daughterboard identity + verdict (#412)** — direct-link BLE keys `mfw` (the modem's own firmware string) and `mst` (1 up, 2 absent, 3 protocol mismatch); `modemAdvisoryText` on both, absent keys decode as no verdict rather than as healthy, one quiet line under the state banner on 2 or 3 only. Seven decode cases mirrored case for case, including the board-with-no-daughterboard case where both keys are omitted. Not on the relay path, and necessarily so: both fault states mean the radio is off. | both | done |
| 2026-09-09 | **ESP-IDF image-header parsing (#773)** — `core/protocol/EspImage.kt` and `Models/EspImage.swift` parse `esp_app_desc_t` (project name, version, chip id, IDF version, build date) and decide whether a picked `.bin` belongs on the connected unit. Nine cases mirrored one for one, including the case the feature exists for: a `base_station` image aimed at the out computer, which both are ESP32-S3 with byte-identical app slots and which nothing downstream separates. Header offsets pinned against three real images built from this repo. | both | done |
| 2026-07-28 | OTA timing contract — shared golden `tests_cpp/fixtures/app_behavior/ota_timeouts.json`, both suites assert it plus the crossesRelay invariant | both | done |
| 2026-07-28 | `OTASession` flow tests. Android has 11 (`OtaSessionTest`) against a scripted peripheral; iOS has none, because `OTASession` reaches concrete `BLEFleet`/`BLEDevice`. Needs a protocol over the 9 members it touches + the fleet lookup behind a closure — the Android design. | Android → iOS | **done 2026-07-30**: `OTALink` protocol (BLEDevice conforms retroactively, zero changes) + `linkLookup` closure init; 9 flow cases ported (`OTASessionFlowTests`) running the REAL timeout table at 1/200 time-scale — full flow incl. the reconnect-swap property, rollback, mid-pump verify-fail, all three timeout branches incl. the FC-window-outlasts-local invariant, size gate, cancel-aborts. Wire-byte and conn-priority assertions stay Android-side by design (encoding is golden-pinned in BLEDevice; iOS has no priority boost). 304/304 suite green |
| 2026-07-29 | Announcer flight-profile tests. The #643 port put callout policy behind an `AnnouncerSpeech` seam, so Android has 15 state-machine cases iOS could not run — its policy was welded to `AVSpeechSynthesizer`. | Android → iOS | **done 2026-07-30**: iOS `FlightAnnouncer` split into policy + `SystemSpeech` engine behind the same `AnnouncerSpeech` shape as Android (isBusy/speak/stop + session status), with injectable clock and unit-system; 17 policy cases ported (`FlightAnnouncerPolicyTests`) — burnout counter incl. jitter tolerance + speed floor, 5 s altitude cadence + deadband, apogee edge + unknown-alt, first-descent-at-5 s-then-10 s, landed haversine + state fallback, STALE gate, disabled tracking, busy-skip vs interrupt, PRELAUNCH reset, enable/disable lifecycle, imperial. Dispatch (#138) + wording (#235) tests unchanged. 321/321 suite. The #390 non-focused-silence case remains Android-only (it lives in dispatch, where iOS's relay path differs structurally) |
| 2026-09-07 | **Hold-up supercap advisory (#1166, PRs #1220/#1221)** — direct-link BLE keys `scap` (V_SCAP volts) and `hu` (the out computer's verdict: 1 charging, 2 charged, 3 not charging, 4 no reading); `HoldupState` + `holdupAdvisoryText` on both, absent keys decode as no verdict, one quiet line under the state banner only on 3 or 4 ("Hold-up backup not charged — x.xx V" / "Hold-up backup sense — no reading"). Three decode tests mirrored case-for-case. Not on the relay path: the LoRa frames do not carry it. | both | done |
| 2026-09-11 | **Simulation screen display units (#1073)** — Android's Simulation screen now reads `LocalUnitSystem` like iOS: the descent-rate field label and value show the display unit (ft/s in imperial) while the stored value stays canonical m/s, and Burnout Speed / Max Altitude format through `UnitFormatter`. Flight Duration is seconds on both. Wire value unchanged (canonical m/s). | Android → iOS | done |
| 2026-09-11 | **Dashboard SNR + hop channel (#1093)** — Android decoded `snr`/`hch` but rendered neither; now shows an SNR row and appends `chN` to the LoRa RSSI value, matching iOS. The three-state hop badge on the state banner is still iOS-only (parked here). Attitude precision aligned to iOS `%.1f°`; the protocol-mismatch drop line recoloured to the caution rung; the mag-cal µT/LSB line shows the decoded scale instead of a hardcoded 0.15. | Android → iOS (SNR row, channel suffix, attitude, mag scale) / iOS-only (hop badge) | partial |
| 2026-09-11 | **3D no-position guard (#1092)** — a log whose EKF never initialised (all-zero Position columns) now reads as no-data on Android instead of drawing a zero-extent line with the markers stacked, matching iOS "No GPS data". Satellite ground imagery and the OFFLINE pill remain iOS-only (deferred). | Android → iOS (degenerate-track guard) / iOS-only (ground imagery, offline pill) | partial |
| 2026-09-11 | **Voice re-routes on foreground BS (#1098)** — Android's binding `combine` now includes `foregroundBSID`, matching iOS. Dormant until a pair switcher calls `setForegroundBS`; wired ahead of that. The foreground-selection control itself is still iOS-only. | Android → iOS (routing) / iOS-only (control) | done |
| 2026-09-12 | **Offline indicator** (#1092 item 3): iOS runs an `NWPathMonitor` and overlays an `OFFLINE` pill on the map and on Drift Cast; Android had no `ConnectivityManager`, no `NetworkCallback` and not even `ACCESS_NETWORK_STATE`. The tile stack already did the right thing when the radio was dead — MapScreen forces `MapLibre.setConnected(true)` and TileProxyServer serves a hatch placeholder on an upstream miss — so the operator saw blank hatching over an unsaved area with no explanation. Android now has `networkOnline()` (ConnectivityManager, keyed on `NET_CAPABILITY_VALIDATED` so a captive portal or a dead wifi reads as offline, matching what the tiles experience) and an `OfflinePill` on MapScreen and DriftCastScreen. Verified rendering on a Pixel 8 with wifi+data disabled. Still iOS-only from the same issue: the satellite ground texture under the 3D scenes (#1092 item 1) | parity |
