# Plans

Design and implementation plans for in-progress work. One file per initiative, named `<issue-number>-<slug>.md` so it sorts naturally and cross-references the GitHub issue.

Plans capture intent and sequence before code lands — the "why" and "in what order". Once the work is done, the plan stays for history; it is not kept in sync with the final implementation.
